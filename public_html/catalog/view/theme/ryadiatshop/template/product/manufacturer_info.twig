{{ header }}
<div id="product-category" class="uk-container uk-container-center">
  <div class="uk-panel uk-panel-box uk-panel-box-secondary">
    <ul class="uk-breadcrumb">
        {% for breadcrumb in breadcrumbs %}
          <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
    </ul>
  </div>


  <div class="uk-panel uk-panel-box uk-panel-box-secondary">
    <div class="uk-grid" data-uk-grid-margin data-uk-grid-match>


      <div class="uk-width-1-1">
        <div class="uk-grid" data-uk-grid-margin data-uk-grid-match="{target:'.block-product'}">
            {% if products %}
              <div class="uk-width-1-1">
                <div class="uk-grid uk-grid-small">
                  <div class="uk-width-medium-2-4"></div>
                  <div class="uk-width-medium-1-4 uk-width-1-2">
                    <div class="form-group input-group input-group-lg">
                      <label class="input-group-addon" for="input-sort">{{ text_sort }}</label>
                      <select id="input-sort" class="form-control" onchange="location = this.value;">
                          {% for sorts in sorts %}
                              {% if sorts.value == '%s-%s'|format(sort, order) %}
                                <option value="{{ sorts.href }}" selected="selected">{{ sorts.text }}</option>
                              {% else %}
                                <option value="{{ sorts.href }}">{{ sorts.text }}</option>
                              {% endif %}
                          {% endfor %}
                      </select>
                    </div>
                  </div>
                  <div class="uk-width-medium-1-4 uk-width-1-2">
                    <div class="form-group input-group input-group-lg">
                      <label class="input-group-addon" for="input-limit">{{ text_limit }}</label>
                      <select id="input-limit" class="form-control" onchange="location = this.value;">
                          {% for limits in limits %}
                              {% if limits.value == limit %}
                                <option value="{{ limits.href }}" selected="selected">{{ limits.text }}</option>
                              {% else %}
                                <option value="{{ limits.href }}">{{ limits.text }}</option>
                              {% endif %}
                          {% endfor %}
                      </select>
                    </div>
                  </div>
                </div>
              </div>

                {% for product in products %}
                  <div class="uk-width-medium-1-4 uk-width-1-2">
                      {% include 'ryadiatshop/template/product/_block_product.twig' with product %}
                  </div>
                {% endfor %}

                {% if pagination %}
                  <div class="uk-width-1-1">
                      {{ pagination }}
                  </div>
                {% endif %}
            {% else %}

              <div class="uk-width-1-1">
                <div class="empty">
                  <p>{{ text_empty }}</p>
                  <div class="buttons clearfix">
                    <div class="pull-right"><a href="{{ continue }}" class="btn btn-primary">{{ button_continue }}</a></div>
                  </div>
                </div>
              </div>
            {% endif %}

        </div>
      </div>
    </div>
  </div>

</div>
<script>
    var slider_1 = new Swiper('#slider-category', {
        simulateTouch:false,
        slidesPerView: 6,
        navigation: {
            nextEl: '.slider-category-button-next',
            prevEl: '.slider-category-button-prev'
        },
        breakpoints: {
            1024: {
                slidesPerView: 6,
                spaceBetween: 10,
            },
            768: {
                slidesPerView: 4,
                spaceBetween: 10,
            },
            640: {
                slidesPerView: 2,
                spaceBetween: 10,
            },
            320: {
                slidesPerView: 2,
                spaceBetween: 10,
            }
        }
    });
</script>
{{ footer }}
